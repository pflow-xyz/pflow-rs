# `goldens.json`

Byte-identical copy of `pflow-xyz/parity/learn/goldens.json`, copied on
2026-09-03. Do not hand-edit; re-copy from pflow-xyz if it regenerates.

sha256 (both sides identical):

```
507794af6cdc4cd5ee8f6f350f34cc742a35ad543102f4b0516da81525b0a328  goldens.json
```

Source: `../pflow-xyz/parity/learn/goldens.json`, generated by go-pflow's
`learn` package (`make learn-goldens`; `parity/learn/gen/main.go`, branch
`tsit5-error-estimate`) against `solver.JSParityOptions()`. Replayed on the JS
side by `pflow-xyz/public/petri-learn_test.ts`; replayed here by
`../tests/parity_goldens.rs`.

## What the goldens cover, and what they don't

`cases[].point1Adjoint` / `point2Adjoint` hold `learn.MSELossAdjoint`'s
reverse-mode loss+grad — pflow-xyz's `petri-learn.js` *does* carry an adjoint
port (`solveAdjoint`/`mseLossAdjoint`, added 2026-08-26) and the JS parity
test replays it against these fields, so **adjoint parity against Go is a
covered, checked contract here**, not something this repo has to take on
faith. (An earlier pass's doc comments in this crate claimed the opposite —
that pflow-xyz never ported adjoint — which was true of an older revision of
`petri-learn.js` but is not true of the copy this README's sha256 pins.)

`optimizers[]` (Rosenbrock, exact float64) and `hinge[]` cover the
optimizer/ranking-loss primitives independent of any ODE solve.

## History: the Rust replay once needed a looser bound than the JS replay (closed 2026-09)

The JS replay compares Go's adaptive-stepper trajectory against JS's own
adaptive stepper — two independent *but design-identical* Tsit5
implementations, both carrying go-pflow's Tsit5 embedded-error-coefficient
fix (`Bhat`'s last entry `-1/66`, not `+1/66` — go-pflow commit `53f7431`),
differing only by ULP-level `math.Pow`/`Math.pow` disagreement. That is why
the JS side can hold `adaptive` cases to `1e-9` relative.

pflow-rs's own `pflow-solver` crate received the same coefficient fix at
commit `1177f4d` (`methods.rs`'s `tsit5()` tableau) — but this README and
`tests/parity_goldens.rs` were written from an earlier measurement and kept
describing the pre-fix behavior (734 accepted steps on `decay` vs go-pflow's
13) for two years' worth of git-blame confusion after the fix had already
landed. Re-measured directly, current `main`: `decay` 13/13, `sir` 15/15,
`tied` 8/8 — accepted-step **counts** now match go-pflow's exactly, not just
similarly. `finalState`/`finalSens` residuals dropped from the previously
documented ~9e-7 (`decay`) / ~1.1e-4 (`sir`) to ~1.9e-14 at the worst
(`sir`'s `finalSens[I][0]`) — solver noise, not a grid-alignment artifact.

Consequences, all now applied unconditionally in `tests/parity_goldens.rs`
(previously only for `exact: true` cases):

- `steps`, `midTime`, `midSens` are asserted for every case — the accepted
  grid position they name is now the same row on both sides.
- `point1`/`point2` MSE/relative-MSE loss+grad and the
  `point1Adjoint`/`point2Adjoint` reverse-mode loss+grad are asserted at
  `EXACT_TOL`. These fold in linear interpolation onto the *dataset's* fixed
  observation times; with identical accepted-step grids on both sides, that
  interpolation now lands on the same two bracketing grid points, so the
  previously-measured "up to ~4.7% relative" divergence (a real artifact of
  two different coarse-grid interpolants, not a bug) no longer applies —
  re-measured at ~1e-12 relative or tighter across `decay`/`sir`/`tied`.
- Adam/Nelder-Mead/`fit` call-trace parity is asserted for every case
  carrying the corresponding section, `decay`/`tied` included: a
  multi-iteration optimizer re-solving at a changing theta no longer
  compounds a per-step grid mismatch, because the grids match at every step,
  not just the first.

The optimizer/ranking-loss sections (`optimizers[]`, `hinge[]`) touch no ODE
solver at all and are asserted bit-for-bit unconditionally (well: at
`EXACT_TOL = 1e-9` relative — see the next paragraph for why that isn't
literal `==`), as before.

## "Exact" isn't literal bit equality, and that's deliberate

Even on `exact: true` cases and the solver-free `optimizers[]`/`hinge[]`
sections, `tests/parity_goldens.rs` compares at `EXACT_TOL = 1e-9` relative
rather than Rust `==`, for two small, understood, unrelated reasons (see the
constant's own doc comment for the full account):

1. `pflow-solver::integrate_vec`'s fixed-step time advances by repeated
   `tcur += dtcur` (`dtcur = 0.01`, not exactly binary-representable), so
   only the clamped final step lands on a request time bit-exactly — every
   other integer-valued dataset time (`t=1,2,...,9`) is reached by linear
   interpolation between two grid nodes a few ULPs off target, not an exact
   hit. Verified directly: `finalState`/`finalSens` on `decay-fixed` (the
   literal-`tf` point) DO match go-pflow to the last bit; `point1`'s MSE
   loss (an 11-point average, 9 of them non-endpoint) differs by ~4e-16
   relative.
2. This crate's Adam bias correction uses `BETA1.powi(t_step)` (exact
   repeated squaring); go-pflow's `math.Pow(beta1, float64(tStep))` is the
   general `exp(y·log(x))` algorithm, not guaranteed bit-identical to
   repeated squaring for a small integer exponent. Observed to diverge by
   1 ULP starting at the very first optimizer step, on the Rosenbrock
   objective — no ODE solver involved, so unambiguously an optimizer-only
   effect.

`1e-9` is twelve orders of magnitude tighter than the JS replay's own bound
for genuinely different adaptive runs (`ADAPTIVE_REL_TOL = 1e-9` there too,
coincidentally the same number for an unrelated reason — see that file's own
comment), chosen to still catch a real regression while not chasing either
source above.

## A real bug this parity pass found (fixed, not worked around)

`optim::nelder_mead`'s `nelder_mead` and `coordinate_descent` both reported
`iterations` as the 0-indexed loop variable's last value (`max_iters - 1`)
on a non-converged, loop-exhausted run, instead of `max_iters` itself —
go-pflow's `nelderMead`/`coordinateDescent` both explicitly `return ...,
opts.MaxIters, false` on that path (the loop variable is used only on the
*converged* return, where the two conventions coincide). This was an
off-by-one in the port, not a parity-tolerance question, and is fixed in
`src/optim/nelder_mead.rs` rather than loosened in the test.
