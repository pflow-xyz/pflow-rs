[package]
name = "pflow-monitoring"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Real-time predictive process monitoring, ported from go-pflow's monitoring package"
keywords = ["petri-net", "process-mining", "monitoring", "prediction"]
categories = ["simulation"]

[dependencies]
pflow-core = { path = "../pflow-core", version = "0.3.0" }
pflow-solver = { path = "../pflow-solver", version = "0.3.0" }
