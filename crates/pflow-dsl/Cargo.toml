[package]
name = "pflow-dsl"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "S-expression DSL for defining token model schemas"
keywords = ["dsl", "s-expression", "petri-net", "token-model", "parser"]
categories = ["parsing", "compilers"]

[dependencies]
pflow-tokenmodel = { path = "../pflow-tokenmodel", version = "0.1.0" }
serde_json = "1"
